# render.yaml - Minimal Configuration

services:
  - type: web
    name: il-lending-services
    runtime: static
    repo: https://github.com/YOUR_USERNAME/YOUR_REPOSITORY_NAME
    branch: main
    buildCommand: echo "Static site - no build needed"
    staticPublishPath: .
    pullRequestPreviewsEnabled: true
    envVars:
      - key: APP_NAME
        value: I&L Lending Services
